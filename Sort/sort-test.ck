Sort s1;
//Sort s2;
s1 => dac;
//s2 => dac.right;

s1.setBubble();
s1.setRandom();
s1.size(500);

//s2.setInsertion();
//s2.setReverse();
//s2.size(10000);

while (true)
{
    50000::samp => now;
}

/*
for (0 => int i; i < samples; i++) {
    <<< s.last() >>>;
    samp => now;
}

<<< "-----------------------", "" >>>;

for (0 => int i; i < samples; i++) {
    <<< s1.last() >>>;
    samp => now;
}

<<< "-----------------------", "" >>>;

for (0 => int i; i < samples; i++) {
    <<< s1.last() >>>;
    samp => now;
}

<<< "-----------------------", "" >>>;

for (0 => int i; i < samples; i++) {
    <<< s1.last() >>>;
    samp => now;
}*/
